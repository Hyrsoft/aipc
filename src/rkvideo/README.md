直接使用luckfox写好的rkmpi文件，基于这个文件，快速搭建vi采集，venc编码的效果，编码得到的h.264码流，交由rtsp服务器或者webrtc进行分发

设计建议
对于 VENC 编码流的处理（RTSP、WebRTC、文件保存），建议采用分发器 + 多消费者模式：

一个获取线程：从 VENC 获取编码流
多个消费者线程：RTSP、WebRTC、文件保存各自独立的消费者
这样的好处是：

零拷贝：所有消费者共享同一内存块（通过 shared_ptr 引用计数）
解耦：任一消费者处理慢不影响其他消费者
扩展性好：新增功能只需注册新消费者